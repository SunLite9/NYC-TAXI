-- Creates dataset (no-op if it already exists)
-- (dataset creation is done by CLI; this file documents schema intent)

-- Optional: the table name the app & trainer use:
-- ${PROJECT}.${DATASET}.yellow_2025_01
